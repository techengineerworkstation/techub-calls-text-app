{
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm turbo build --filter=@telnyx-app/web",
  "outputDirectory": "packages/web/.next",
  "framework": "nextjs"
}
